TRISH AI Engine (FastAPI)
-------------------------
This is a simple FastAPI service with a mock /match endpoint.

To run locally:
  $ python -m venv venv
  $ source venv/bin/activate
  $ pip install -r requirements.txt
  $ uvicorn app:app --reload --port 8000
